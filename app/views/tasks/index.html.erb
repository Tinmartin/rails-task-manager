<% @tasks.each do |task| %>
  <%= link_to task.description, task_path(task) %>

<%= link_to "Delete", task_path(task),
      method: :delete,
      data: { confirm: "Are you sure?" } %>
      <br>

<% end %>
